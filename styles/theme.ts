interface Theme {
  fontColor: string;
  bgColor: string;
}

export const lightTheme: Theme = {
  fontColor: '#000',
  bgColor: '#fff',
};

export const darkTheme: Theme = {
  fontColor: '#fff',
  bgColor: '#121212',
};
